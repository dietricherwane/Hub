<%#= javascript_include_tag "ecommerces"  if Hub::Application.assets.find_asset("ecommerces.js") %>

<!-- PAGE HEADER -->
<div class="container page-header ph-bg2">
  <div class="row">
    <div class="twelve columns">
      <p class="page-title">POS particuliers de: <%= @merchant_pos_account.company %></p>
      <ul class="breadcrumbs">
        <li><a><%= render "shared/display_user_name" %></a></li>
        <li><a href="<%= root_path %>">Accueil</a></li>
      </ul>
    </div>
  </div>
</div>
<!-- END PAGE HEADER -->

<div class="container main_content">
  <div class="row">
    <div class="twelve columns">
      <%= render "shared/flash" %>
      <% if @private_pos_accounts.blank? %>
        <h3>Aucune donnée à afficher</h3>
      <% else %>
        <div id = "toggle-style-2">
          <% @private_pos_accounts.each do |private_pos_account| %>
            <% @private_pos_account = private_pos_account %>
            <div class = "tgg-item">
              <%= render "list_private_pos_account", locals: {private_pos_account: @private_pos_account} %>
            </div>
          <% end %>
        </div>

        <%= paginate @private_pos_accounts, :params => {:controller => :registrations, :action => :list_private_pos_account} %>
      <% end %>
    </div>
  </div>
</div>
